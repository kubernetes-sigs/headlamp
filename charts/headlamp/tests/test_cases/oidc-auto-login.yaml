# This is a test case for OIDC auto-login with created secret.
config:
  oidc:
    secret:
      create: true
      name: oidc
    clientID: "testClientId"
    clientSecret: "testClientSecret"
    issuerURL: "testIssuerURL"
    scopes: "testScope"
    autoLogin: true
