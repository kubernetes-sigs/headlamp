# This is a test case for apiServerEndpoint in the Headlamp deployment.
# Tests the behavior when a custom API server endpoint is configured.
config:
  apiServerEndpoint: "https://kube-oidc-proxy.example.com:443"
