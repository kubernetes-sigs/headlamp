# Test case: metrics enabled with ServiceMonitor
config:
  inCluster: true

metrics:
  enabled: true
  serviceMonitor:
    enabled: true
    labels:
      release: kube-prometheus-stack
    interval: "15s"
    scrapeTimeout: "5s"
